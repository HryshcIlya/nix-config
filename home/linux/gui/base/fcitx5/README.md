# fcitx5 - IME

## Configuration

- `profile` → Symlink will be created at: `~/.config/fcitx5/profile`
  - Configured for simple US (English) and RU (Russian) layout switching.
  - Group: Default (contains both layouts)
